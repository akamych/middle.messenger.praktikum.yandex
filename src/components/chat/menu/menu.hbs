<div class="chatMenu">
    <b>{{ title }}</b>
    <div class="avatar">
        {{#if avatar}}
            <img src={{ avatar }} />
        {{/if}}
    </div>
    <nav>
        {{{ listUsers }}}
        {{{ clearChat }}}
        {{{ deleteChat }}}
    </nav>
</div>
