<li class="feedChat">
    <div class="avatar">
    {{#if avatar }}
        <img src="{{ avatar }}" />
    {{/if}}
        
    </div>
    <b>{{ title }}</b>
    <p>{{ last_message }}</p>
    {{#if unread_count }}
        <span class="new">{{ unread_count }}</span>
    {{/if}}
</li>
