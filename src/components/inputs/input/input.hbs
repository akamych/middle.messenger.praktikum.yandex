<label>
    <input
        type = {{ type }}
        name = {{ name }}
        placeholder = {{ label }}
        {{#if value }} 
            value = {{ value }}
        {{/if}}
    />
    <span class="label">{{ label }}</span>
</label>