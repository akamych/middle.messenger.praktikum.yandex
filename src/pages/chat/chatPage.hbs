<main class="chat">
    
    <aside>
        {{{ feed }}}
        {{{ usersList }}}
    </aside>

    <section class="chatMessages">
        {{{ menu }}}
        {{{ messages }}}
        {{{ chatSend }}}
    </section>

</main>
